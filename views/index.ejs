<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="Jonathan Anthony">
    <meta name="description" content="To Do List to use every day">
    <meta name="keywords" content="toDo List, List">

    <title>To Do List</title>

    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;700&display=swap" rel="stylesheet">
    <link href='https://css.gg/trash.css' rel='stylesheet'>
    <link
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/simplebar@latest/dist/simplebar.css"
    >
    <!-- own css -->
    <link rel="stylesheet" href="style.css">
    
</head>
<body>
    <main class="toDo">
        <div class="toDo__date">
            <h1><%= day %></h1>
            <p><%= fullDate %></p>
        </div>

        <div class="toDo__newTask">
            <form action="/" method="post">
                <input type="text" name="task" autocomplete="off">
                <button type="submit" class="btn btn-add">+</button>
            </form>
        </div>

        <hr>

        <div class="toDo__tasks" data-simplebar data-simplebar-scrollbar-max-size="100">

            <% arr.forEach(element => { %>
             <div class="toDo__task-box">
                <p><%= element.description %> </p>
                
                <form action="/delete" method="post">
                    <button type="submit" name="id" value="<%= element._id %>" class="btn btn-del">
                        <i class="gg-trash"></i>
                    </button>
                </form> 
             </div>
            <% }) %>

        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/simplebar@latest/dist/simplebar.min.js"></script>
</body>
</html>